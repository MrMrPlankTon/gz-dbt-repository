
   # schema.yml

 version: 2

 sources:
   - name: gz_raw_data
     database: main-bloom-484811-f4 #data set
     schema: gz_raw_data #table
     loader: bigquery 
     description: "projet_greenweez"
       
     tables:
       - name: sales 
         identifier : raw_gz_sales
       - name: product
         identifier : raw_gz_product
       - name: ship
         identifier : raw_gz_ship